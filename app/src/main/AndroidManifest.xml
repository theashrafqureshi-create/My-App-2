<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.google.com/tools">

    <!-- इंटरनेट और नेटवर्क स्टेट की परमिशन -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    
    <!-- Android 14 वीपीएन फ़ोरग्राउंड सर्विस के लिए सबसे सटीक परमिशन -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_VPN" />
    
    <!-- Android 13/14 नोटिफिकेशन रनटाइम परमिशन -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".AppConfig"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="WhatsApp VPN"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:targetApi="34">

        <activity
            android:name="com.ashraf.whatsappvpn.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- हमारी Shadowsocks VPN Service -->
        <!-- 🎯 यहाँ foregroundServiceType को "vpn" कर दिया है, जो एंड्रॉइड 14 का ऑफिशियल नियम है -->
        <service
            android:name="com.ashraf.whatsappvpn.service.ShadowsocksVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="vpn"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

    </application>

</manifest>
