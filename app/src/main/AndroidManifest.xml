<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.google.com/tools">

    <!-- बुनियादी इंटरनेट और नेटवर्क परमिशन -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- 🚀 फिक्स #1: एंड्रॉइड 14+ के लिए सही वीपीएन फोरग्राउंड परमिशन जोड़ी -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_VPN" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name="com.ashraf.whatsappvpn.AppConfig"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="WhatsApp VPN"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:targetApi="34">

        <!-- मेन स्क्रीन (Launcher) -->
        <activity
            android:name="com.ashraf.whatsappvpn.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- सेटिंग्स स्क्रीन -->
        <activity
            android:name="com.ashraf.whatsappvpn.ui.SettingsActivity"
            android:exported="false" />

        <!-- 🚀 फिक्स #2: सर्विस का नाम नया V2RayVpnService किया और टाइप को "vpn" पर लॉक कर दिया -->
        <service
            android:name="com.ashraf.whatsappvpn.service.V2RayVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="vpn"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

    </application>

</manifest>
